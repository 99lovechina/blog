##一、TCP网络编程的概念
1.1 字节序：由于不同类型的CPU主机字节序不一定相同，所有发送或是接收时，数据可能出现不一致的情况，所以数据在网络中传输，必须是大端序
        //本机字节序的判断
        int m = 0x12345678;
        char *p = (char*)&m;
        if(*p == 0x78){
            printf("this is 小端\n");
        }else if(*p == ox12){
            printf("this is 大端\n");
        }
